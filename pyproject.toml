[project]
name = "fz-git-file-histrory-logo"
version = "0.1.0"
description = "Logo generator for FZ Git History extension"
requires-python = ">=3.8"
dependencies = [
    "pillow>=10.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
